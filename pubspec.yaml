name: 'automated_testing_framework_plugin_firebase_storage'
description: "Library to use Google's Firebase Storage as a mechanism for automated tests and test results."
homepage: 'https://github.com/peiffer-innovations/automated_testing_framework_plugin_firebase_storage'
version: '5.0.0+8'

environment: 
  sdk: '>=2.17.0 <3.0.0'

dependencies: 
  automated_testing_framework: '^4.0.2'
  convert: '^3.0.2'
  firebase_auth: '^3.4.2'
  firebase_core: '^1.19.2'
  firebase_storage: '^10.3.2'
  flutter: 
    sdk: 'flutter'
  form_validation: '^2.1.0+10'
  json_class: '^2.1.2+8'
  logging: '^1.0.2'
  static_translations: '^2.1.1+7'

dev_dependencies: 
  flutter_test: 
    sdk: 'flutter'


flutter: 
  uses-material-design: true
